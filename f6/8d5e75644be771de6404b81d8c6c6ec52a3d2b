{"category": "Changed", "changelog_entry": "Tests are now more reliable and accurate due to improved test scripts that check for package installation and version availability before proceeding.", "summary": "Tests were refactored to improve reliability and accuracy. The main changes include switching to `errexit` instead of `xtrace` in the Vagrantfile, and updating several test scripts to check for package installation and version availability before proceeding. This ensures that tests complete successfully even if required packages are already installed.", "version": 1}
