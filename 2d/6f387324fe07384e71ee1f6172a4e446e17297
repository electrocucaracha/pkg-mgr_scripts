{"category": "Changed", "changelog_entry": "Updated the default Go version in the build script from 1.13.5 to 1.14, requiring users to rebuild their projects to utilize the new version.", "summary": "The default Go version in the build script has been updated from 1.13.5 to 1.14. This change requires no migration steps, but users must rebuild their projects to use the new version. The Go version is now set to 1.14 by default, which may affect projects that rely on specific Go versions.", "version": 1}
