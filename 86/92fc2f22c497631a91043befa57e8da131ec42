{"category": "Changed", "changelog_entry": "Enabled users to configure default address pools for Docker containers through the introduction of a new daemon option.", "summary": "Docker default-address-pools daemon option was added to allow users to configure default address pools for Docker containers. This change introduces a new environment variable PKG_DOCKER_DEFAULT_ADDRESS_POOLS and updates the /etc/docker/daemon.json configuration file to include the default address pools. The change does not break existing behavior, but users must configure the new option to take advantage of it.", "version": 1}
