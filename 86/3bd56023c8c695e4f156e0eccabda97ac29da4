{"category": "Changed", "changelog_entry": "Docker user namespace mapping is now enabled by default, allowing users to remap the user namespace and potentially requiring migration steps to ensure compatibility with existing Docker configurations.", "summary": "Docker user namespace mapping is now enabled by default in the build script. This change affects Docker daemon configuration, specifically the `/etc/docker/daemon.json` file, and enables user namespace remapping. The `userns-remap` option is set to \"default\" when user namespace mapping is enabled. This change may require migration steps to ensure compatibility with existing Docker configurations.", "version": 1}
