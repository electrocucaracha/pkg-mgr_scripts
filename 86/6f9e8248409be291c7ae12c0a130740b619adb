{"category": "Fixed", "changelog_entry": "The qat-driver scripts now stabilize their behavior in minimal environments by skipping installation and validation logic if the lspci command is missing, matching the behavior when no QAT hardware is detected and improving robustness on systems without PCI utilities.", "summary": "The qat-driver scripts now skip installation and validation logic if the lspci command is missing, improving robustness on systems without PCI utilities and avoiding unnecessary error messages. This change addresses failures in minimal environments where lspci is not installed. The scripts now match the behavior when no QAT hardware is detected, providing a more reliable experience for users.", "version": 1}
