{"category": "Removed", "changelog_entry": "The UART workaround has been eliminated, necessitating configuration updates from users who previously relied on it.", "summary": "The UART workaround has been removed from the Vagrantfiles, which may break behavior for users who relied on this workaround. The change does not introduce any API or CLI contract changes, nor does it affect security or config schema. Users who previously used this workaround will need to update their configurations to accommodate the change.", "version": 1}
