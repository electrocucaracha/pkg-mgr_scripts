{"category": "Changed", "changelog_entry": "Optimized ftrace setup instructions to improve compatibility with various architectures by adding a conditional check for the setcap command.", "summary": "Ftrace setup instructions were updated to ensure compatibility with various architectures. The change adds a conditional check for the `setcap` command, allowing the script to proceed without it if not available. This update does not introduce any breaking behavior or migration requirements, and the API or CLI contract remains unchanged.", "version": 1}
