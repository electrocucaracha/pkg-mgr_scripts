{"category": "Removed", "changelog_entry": "Simplified the execution of vagrant commands by requiring sudo privileges to run vagrant up and destroy commands, necessitating updates to scripts to use sudo.", "summary": "The newgrp instruction in check.sh has been removed, requiring sudo privileges to run vagrant commands. This change affects API contract and CLI usage, as users must now use sudo to run vagrant up and destroy commands. Migration steps involve updating scripts to use sudo, and there is no security impact. The primary user outcome is a simplified and more secure way to run vagrant commands.", "version": 1}
