{"category": "Added", "changelog_entry": "Optimized the Docker container build process by including README.md, Vagrantfile, and validate.sh, while continuing to exclude .DS_Store, with no migration steps or API changes required.", "summary": "The Docker container now includes fewer files, with README.md, Vagrantfile, and validate.sh added to the build process, while .DS_Store is still excluded. This change affects users who rely on these files in their development environment, requiring no migration steps or API changes. The security impact is minimal, with no changes to the config schema.", "version": 1}
