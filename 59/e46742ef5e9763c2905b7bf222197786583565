{"category": "Added", "changelog_entry": "Enabled the GUI option to bypass the VBoxHeadless issue on macOS, allowing CI/CD workflows to proceed without interruption.", "summary": "The GUI option is enabled to bypass the VBoxHeadless issue on macOS, which previously caused CI/CD workflows to fail. This change applies a workaround by setting `v.gui` to `true` in Vagrantfiles, affecting multiple workflows. The fix is temporary and will be removed once the underlying issue is resolved.", "version": 1}
