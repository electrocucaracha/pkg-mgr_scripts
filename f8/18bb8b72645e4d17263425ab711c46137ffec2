{"category": "Changed", "changelog_entry": "Upgraded the Go language version used in the build process to 1.14.2.", "summary": "The Go language version in the build process has been updated to 1.14.2. This change affects the build process, as the version of Go used to build the project is now explicitly specified. The build process will now use the updated version of Go, but no migration steps are required.", "version": 1}
