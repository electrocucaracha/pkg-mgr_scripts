{"category": "Added", "changelog_entry": "Updated the Docker Ubuntu script to refresh the package list before installing uidmap, ensuring users with outdated package lists can successfully install Docker on Ubuntu systems.", "summary": "The Docker Ubuntu script has been updated to include a call to `sudo apt-get update` before installing `uidmap`. This change is relevant to users who rely on the script to install Docker on Ubuntu systems. The update ensures that the package list is refreshed before installing, which may be necessary for users who have not run `apt-get update` recently.", "version": 1}
