{"category": "Changed", "changelog_entry": "Enabled users to specify container images for various distributions, requiring updated configuration with new image fields in the `distros_supported.yml` file and corresponding changes to the `ci/update_distros.sh` script.", "summary": "Images support has been added to the system, allowing users to specify container images for various distributions. This change affects the configuration of supported distributions, with new image fields added to the `distros_supported.yml` file and corresponding updates to the `ci/update_distros.sh` script. Users must update their configuration to include the new image fields for each distribution.", "version": 1}
