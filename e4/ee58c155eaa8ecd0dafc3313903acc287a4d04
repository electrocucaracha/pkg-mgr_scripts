{"category": "Changed", "changelog_entry": "The QEMU installation process on QEMU has been optimized for the opensuse case by automatically installing the necessary RPMs for libpmem if they are not already present.", "summary": "QEMU installation on QEMU has been improved by modifying the main.sh script to handle package installation for the opensuse case. The change introduces a dependency check for libpmem, and if not installed, it downloads and installs the necessary RPMs. This update ensures a smoother installation process for users on opensuse.", "version": 1}
