{"category": "Changed", "changelog_entry": "Standardized distributions' support to a centralized list stored in a single YAML file, which now drives Vagrant environment configurations across the project.", "summary": "Distributions' support has been centralized and standardized across the project. The list of supported distributions is now stored in a single YAML file, `distros_supported.yml`, and used to configure Vagrant environments. This change affects the Vagrantfile configurations in various directories, including `docker`, `golang`, and `vagrant`, which now reference the centralized list of supported distributions.", "version": 1}
