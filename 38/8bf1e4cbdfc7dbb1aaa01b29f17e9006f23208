{"category": "Changed", "changelog_entry": "The Ubuntu install command now suppresses the installation of recommended packages by default, which may impact users relying on these packages.", "summary": "The Ubuntu install command has been modified to include the `--no-install-recommends` flag, which prevents the installation of recommended packages. This change affects the behavior of the installer and may impact users who rely on these packages. The modification also adds a flag to suppress output in debug mode, but this change does not affect the API or CLI contract.", "version": 1}
