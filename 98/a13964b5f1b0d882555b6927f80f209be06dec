{"category": "Fixed", "changelog_entry": "Stabilized CI matrix generation to correctly handle full distro blacklists, preventing malformed queries and ensuring no CI failures when no supported distros remain.", "summary": "The CI matrix generation now correctly handles the case where all distro aliases are blacklisted for a script, preventing malformed queries and CI failures. This change ensures that when no supported distros remain, the query returns no rows instead of producing an error. The fix modifies the jq query generation logic to account for the full blacklist scenario.", "version": 1}
