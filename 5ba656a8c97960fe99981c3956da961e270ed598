{"category": "Fixed", "changelog_entry": "The script now tolerates apt install failures, allowing VirtualBox tools to be usable even if the install step fails in container environments without kernel headers.", "summary": "The script now tolerates apt install failures, allowing VirtualBox tools to be usable even if the install step fails in container environments without kernel headers. This change improves compatibility with minimal containers. Breaking behavior or migration requirements are not affected, and the API or CLI contract remains unchanged. The config schema and security impact also remain unaffected.", "version": 1}
