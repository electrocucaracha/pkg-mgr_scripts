{"category": "Added", "changelog_entry": "Enabled correct package installation and update on Ubuntu systems, including ca-certificates, by adding the necessary repository and key.", "summary": "The skopeo script on Ubuntu has been updated to correctly install and update the necessary packages. This change involves modifying the script to handle package installation and update on Ubuntu systems, which is a breaking change for users on this platform. The script now correctly installs and updates the required packages, including ca-certificates, after adding the necessary repository and key.", "version": 1}
