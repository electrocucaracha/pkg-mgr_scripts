{"category": "Removed", "changelog_entry": "Simplified test maintenance by replacing legacy shell-based validation scripts with unified, feature test-compatible scripts for Docker, Helm, Kind, and kubectl, and removing legacy test scripts for act, aws, cni-plugins, and kubectl plugins, requiring downstream consumers to migrate to the new test structure.", "summary": "Legacy shell-based test scripts were replaced with unified, feature test-compatible scripts for Docker, Helm, Kind, and kubectl. This change streamlines test maintenance and aligns with devcontainer features test best practices. BREAKING CHANGE: Legacy test scripts for act, aws, cni-plugins, and kubectl plugins are removed; downstream consumers must migrate to the new test structure.", "version": 1}
