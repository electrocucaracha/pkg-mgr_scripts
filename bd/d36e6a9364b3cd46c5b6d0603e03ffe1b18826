{"category": "Added", "changelog_entry": "Enabled code style and spelling error detection on the master branch through the addition of the Reviewdog GitHub action, which runs on push events and requires a GitHub token stored as a secret.", "summary": "Reviewdog GitHub action has been added to enforce code style and detect spelling errors on the master branch. This introduces a new workflow that runs on push events to the master branch, using the reviewdog/action-misspell action to check for misspellings. The action requires a GitHub token to be stored as a secret, which is used to authenticate the action.", "version": 1}
