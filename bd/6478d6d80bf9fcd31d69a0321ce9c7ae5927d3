{"category": "Changed", "changelog_entry": "TravisCI checks now persist vagrant instances after test failures, improving test reliability and reducing instance loss.", "summary": "TravisCI checks now use the `--no-destroy-on-error` flag, preventing vagrant instances from being destroyed on test failures. This change affects the vagrant up and destroy steps, and users should note that instances will persist after test failures. The primary outcome is improved test reliability and reduced instance loss.", "version": 1}
