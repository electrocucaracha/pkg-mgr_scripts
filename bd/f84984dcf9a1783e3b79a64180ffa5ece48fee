{"category": "Added", "changelog_entry": "Enabled users to interact with the registry API through Docker's plugin regctl, which is now installed and configurable by default.", "summary": "Docker's plugin regctl was added to the scripts, allowing users to interact with the registry API. The plugin installation process was also integrated into the main script, automatically installing regctl when it's not present. This change requires no migration steps, but users should note that the regctl version is now configurable through the PKG_REGCLIENT_VERSION variable.", "version": 1}
