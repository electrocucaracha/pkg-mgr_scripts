{"category": "Changed", "changelog_entry": "The workaround for the VBoxHeadless issue in macOS now only applies when the VirtualBox version is 6.1.28r147628, thanks to the latest VirtualBox release fixing the underlying issue.", "summary": "The workaround for the VBoxHeadless issue in macOS has been updated to use a conditional statement based on the VirtualBox version. This change is in response to the latest VirtualBox release fixing the issue. The workaround now only applies when the VirtualBox version is 6.1.28r147628, allowing for a more targeted fix.", "version": 1}
