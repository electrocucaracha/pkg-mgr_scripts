{"category": "Changed", "changelog_entry": "Simplified the kubectl installation script to use a more reliable URL for retrieving the latest version from dl.k8s.io instead of storage.googleapis.com.", "summary": "The kubectl installation script has been improved to use a more reliable URL for retrieving the latest kubectl version. The script now fetches the stable version from dl.k8s.io instead of storage.googleapis.com, and the curl commands have been simplified. This change does not introduce any breaking behavior or migration requirements, and the API or CLI contract remains unchanged.", "version": 1}
