{"category": "Changed", "changelog_entry": "Upgraded the actions/cache dependency to version 4.0.2, which may require a workflow update to utilize the new version.", "summary": "The version of the actions/cache dependency was updated from 4.0.1 to 4.0.2, which may require a workflow update to use the new version. This change does not affect the API or CLI contract, and there is no security impact. The updated dependency is used in the .github/workflows/distros.yml file, where the actions/cache step now uses the new version.", "version": 1}
