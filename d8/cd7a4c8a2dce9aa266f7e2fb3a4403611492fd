{"category": "Changed", "changelog_entry": "Optimized support for openSUSE by enabling the distribution through modifications to the installer command and removing exclusion from the os-blacklist.conf file, resulting in improved support for the platform.", "summary": "The nodejs script now supports openSUSE by adding a case for the SUSE distribution and modifying the installer command to use zypper. The script also removes the os-blacklist.conf file, which previously excluded openSUSE. This change does not introduce any breaking behavior or migration requirements, and the API or CLI contract remains unchanged. The primary user outcome is improved support for openSUSE.", "version": 1}
