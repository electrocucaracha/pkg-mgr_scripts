{"category": "Changed", "changelog_entry": "Simplified the management of GitHub Workflows by extracting filters into a separate YAML file, resulting in improved maintainability and flexibility for users.", "summary": "Filters for GitHub Workflows were extracted from the on-demand workflow and placed into a separate YAML file, allowing for easier management and updates of filter configurations. This change does not introduce any breaking behavior or migration requirements, and the API or CLI contract remains unchanged. The primary user outcome is improved maintainability and flexibility in managing workflow filters.", "version": 1}
