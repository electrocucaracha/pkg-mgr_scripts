{"category": "Changed", "changelog_entry": "Enabled the qemu script on CentOS 8 to require perl as an additional dependency, necessitating its installation alongside existing requirements without introducing any API or CLI changes.", "summary": "perl dependency added to CentOS 8 for qemu script, affecting installation requirements. This change introduces a new dependency, perl, which must be installed along with existing dependencies. No API or CLI changes are introduced, and no security impact is noted. The qemu script now requires perl on CentOS 8, but migration steps are not specified.", "version": 1}
