{"category": "Changed", "changelog_entry": "Upgraded the GitHub action labeler to version 4.3.0, enabling workflows that utilize this action to take advantage of the latest features and improvements without requiring significant migration steps.", "summary": "The GitHub action labeler was updated from version 4.2.0 to 4.3.0, which may affect workflows that use this action. The change is a minor version update, so it should not require significant migration steps. The updated action is now used in the triage workflow, replacing the older version.", "version": 1}
