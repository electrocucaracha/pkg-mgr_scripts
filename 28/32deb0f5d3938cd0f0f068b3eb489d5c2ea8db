{"category": "Added", "changelog_entry": "Enabled installation of Kubernetes command-line tool and Krew through the addition of a kubectl installation script that installs and configures them for use.", "summary": "kubectl installation script added to facilitate installation of Kubernetes command-line tool and Krew, a tool for discovering, installing, and managing kubectl plugins. The script installs kubectl and Krew, and configures the system to use them. This change affects users who need to install and manage kubectl plugins, and may require migration steps to update existing installations.", "version": 1}
