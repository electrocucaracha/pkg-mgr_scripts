{"category": "Changed", "changelog_entry": "Improved error handling for the _vercmp function now enables it to print a warning and exit with a non-zero status code when encountering unrecognized version comparison operations.", "summary": "The _vercmp function, used for comparing versions, was refactored to handle unrecognized operations more gracefully. Instead of dying with an error message, it now prints a warning and exits with a non-zero status code. This change affects multiple scripts, but does not introduce any breaking behavior or require migrations. The primary outcome is improved error handling for unexpected version comparison operations.", "version": 1}
