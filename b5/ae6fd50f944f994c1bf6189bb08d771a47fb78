{"category": "Changed", "changelog_entry": "The vagrant script now dynamically selects the correct architecture for clear-linux-os packages based on the detected CPU architecture.", "summary": "The vagrant script now determines the CPU architecture and uses it to select the correct vagrant package. This change affects the package name for clear-linux-os, which now uses the get_cpu_arch function to dynamically select the correct architecture. The get_cpu_arch function is added to the vagrant script and uses the uname command to determine the CPU architecture.", "version": 1}
