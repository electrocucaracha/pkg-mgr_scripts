{"category": "Changed", "changelog_entry": "The default behavior of the CNI plugin installation process has been modified to no longer include Flannel CNI installation, requiring users to update scripts and configurations that relied on this installation.", "summary": "Flannel CNI installation is disabled by default, removing it from the CNI plugin installation process. This change affects the config schema, as the `PKG_FLANNEL_VERSION` environment variable is no longer used, and the `PKG_CNI_PLUGINS_INSTALL_FLANNEL` variable controls whether Flannel CNI is installed. Migration steps are required to update scripts and configurations that relied on Flannel CNI installation.", "version": 1}
