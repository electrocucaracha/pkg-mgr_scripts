{"category": "Removed", "changelog_entry": "Optimized the default Docker configuration by eliminating the native.cgroupdriver option, which may necessitate manual updates to custom daemon.json configurations for users relying on this option.", "summary": "The native.cgroupdriver docker option has been removed from the default configuration. This change affects users who rely on this option to configure their Docker daemon. The removal does not introduce any breaking behavior, API or CLI changes, or security impact, but it may require manual updates to custom daemon.json configurations.", "version": 1}
