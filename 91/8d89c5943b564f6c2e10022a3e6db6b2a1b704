{"category": "Changed", "changelog_entry": "Moved the installation of kubectl binaries to the system's /usr/local/bin directory, requiring users to update their installation process accordingly.", "summary": "The destination for installing kubectl binaries has been changed from a temporary directory to the system's /usr/local/bin directory. This change affects users who rely on the script to install kubectl, as they will now need to update their installation process to accommodate the new installation location. The change does not impact the API or CLI contract, and there is no security implication.", "version": 1}
