{"category": "Changed", "changelog_entry": "Optimized the GitHub labeler action to require a token with read and write permissions to pull requests, affecting the workflow triage.yml which uses the action to assign labels.", "summary": "The GitHub labeler action was updated to include a license identifier and improved permissions for accessing repository contents. The action now requires a GitHub token with read and write permissions to pull requests. This change affects the workflow triage.yml, which uses the labeler action to assign labels to pull requests.", "version": 1}
