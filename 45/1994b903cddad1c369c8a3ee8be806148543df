{"category": "Changed", "changelog_entry": "Updated pinned versions to reflect the latest dependencies, bumping PKG_KIND_VERSION from 0.10.0 to 0.11.0 without introducing breaking behavior or requiring migration.", "summary": "Pinned versions in the `pinned_versions.env` file were updated to reflect the latest versions of various dependencies. Specifically, the `PKG_KIND_VERSION` was bumped from 0.10.0 to 0.11.0. This change does not introduce any breaking behavior or migration requirements, nor does it affect the API or CLI contract. The security impact is minimal, and the config schema remains unchanged.", "version": 1}
