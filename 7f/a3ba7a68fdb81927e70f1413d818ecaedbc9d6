{"category": "Changed", "changelog_entry": "Krew now fetches the version from the latest GitHub release instead of using the PKG_KREW_VERSION environment variable.", "summary": "The PKG_KREW_VERSION environment variable was externalized and its value is now fetched from the latest GitHub release. This change affects scripts that install and validate Krew, which now use the externalized version. The scripts also check the installed Krew version against the expected one.", "version": 1}
