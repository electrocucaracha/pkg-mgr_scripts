{"category": "Changed", "changelog_entry": "The vagrant plugin installation process now installs vagrant-reload and removes the explicit version requirement for vagrant-libvirt, introducing a new dependency that requires users and maintainers to update their plugins accordingly to avoid any issues.", "summary": "The vagrant plugin installation process was updated to install vagrant-reload and remove the explicit version requirement for vagrant-libvirt. This change breaks the previous version-specific behavior and introduces a new dependency. Users and maintainers should update their plugins accordingly to avoid any issues.", "version": 1}
