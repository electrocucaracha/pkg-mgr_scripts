{"category": "Fixed", "changelog_entry": "resolved the race condition in the check-dictionary job by isolating dictionary updates with a distinct branch name.", "summary": "The check-dictionary job now uses a distinct branch name to prevent a race condition, where multiple updates could interfere with each other. This change affects the PR_BRANCH_NAME variable in the update workflow, which is now set to \"dictionary-update-${PR_ID}\" instead of a generic \"versions-update-${PR_ID}\". This update ensures that dictionary updates are properly isolated and do not conflict with other updates.", "version": 1}
