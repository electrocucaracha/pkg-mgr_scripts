{"category": "Fixed", "changelog_entry": "Stabilized the test job's command to correctly handle script names with special characters or spaces, and set the base image statically to the Ubuntu devcontainer image for improved consistency across runs.", "summary": "The test job's command now correctly handles script names with special characters or spaces by assigning the script to an environment variable. The base image is also set statically to the Ubuntu devcontainer image, improving consistency across runs and reducing test flakiness. This change does not introduce any breaking behavior or migration requirements.", "version": 1}
