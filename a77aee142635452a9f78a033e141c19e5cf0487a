{"category": "Fixed", "changelog_entry": "Stabilized compatibility across environments by providing a local sudo fallback when running as root without sudo, and improved the exportfs check to verify both the command's presence in PATH and its existence at /usr/sbin/exportfs, preventing false negatives and ensuring robust installation and testing in diverse container setups.", "summary": "Scripts that run as root without sudo now have a local fallback, ensuring compatibility across environments. The exportfs check in test scripts is improved to verify both the command's presence in PATH and its existence at /usr/sbin/exportfs. This prevents false negatives when exportfs is installed but not in PATH, improving robustness for installation and testing in diverse container setups.", "version": 1}
