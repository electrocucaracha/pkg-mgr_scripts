{"category": "Added", "changelog_entry": "Images are now automatically compressed in pull requests through the addition of a GitHub Actions workflow.", "summary": "A GitHub Actions workflow for compressing images was added to automatically compress images in pull requests. This change affects users who push images with .jpg, .jpeg, .png, or .webp extensions, as their images will be compressed before being reviewed. The workflow uses the `calibreapp/image-actions` action to compress images, and it requires the `GITHUB_TOKEN` secret to be set.", "version": 1}
