{"category": "Changed", "changelog_entry": "Improved the robustness of CNI plugin validation by checking for the existence of the plugins folder before attempting to validate its contents.", "summary": "CNI plugin validation was refactored to check for the existence of the plugins folder before attempting to validate its contents. This change ensures that the validation process is more robust and less prone to errors. The validation of the CNI plugin folder creation was moved to the beginning of the script, and the original check was removed.", "version": 1}
