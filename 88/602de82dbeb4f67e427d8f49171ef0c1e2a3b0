{"category": "Changed", "changelog_entry": "Enabled kubectl-convert tool for converting Kubernetes resources, now downloadable alongside kubectl through the kubectl script.", "summary": "kubectl script now downloads and installs kubectl-convert, a tool for converting Kubernetes resources, alongside kubectl. The script's installation and validation logic have been updated to accommodate the new tool. The changes do not introduce breaking behavior or require migration steps. The kubectl-convert tool is downloaded from a Kubernetes release repository, and its installation is validated in addition to kubectl's.", "version": 1}
