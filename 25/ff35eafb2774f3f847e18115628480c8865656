{"category": "Changed", "changelog_entry": "Updated the pip URL for Python 3.5 to use a version-agnostic URL, requiring users to update scripts/pip/main.sh to the new URL to continue installing pip.", "summary": "The pip URL for Python 3.5 was updated to use pip instead of the version-specific URL. This change affects users who need to install pip for Python 3.5, requiring them to use the updated URL. The migration step is to update the pip URL in scripts/pip/main.sh to the new version-agnostic URL.", "version": 1}
