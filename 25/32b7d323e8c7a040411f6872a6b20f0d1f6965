{"category": "Added", "changelog_entry": "Enabled tox as a package manager option, allowing users to install dependencies using the tox tool.", "summary": "Tox support was added to the package manager options, allowing users to install dependencies using the tox tool. The tox option is now included in the list of supported package managers, alongside pip, bindep, and docker-compose. This change does not introduce any breaking behavior or migration requirements, nor does it affect the API or CLI contract, config schema, or security.", "version": 1}
