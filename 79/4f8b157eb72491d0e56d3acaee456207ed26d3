{"category": "Changed", "changelog_entry": "Updated the actions/cache dependency to version 3.3.0, which does not change the API or CLI contract, has no security impact, and does not require users to update their workflows.", "summary": "The version of the actions/cache dependency was updated from 3.2.6 to 3.3.0, which may require users to update their workflows to use the new version. The update does not change the API or CLI contract, and there is no security impact. The config schema remains unchanged, and users do not need to take any migration steps.", "version": 1}
