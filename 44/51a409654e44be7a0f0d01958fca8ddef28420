{"category": "Changed", "changelog_entry": "Fly now uses the latest tag version from the concourse/concourse GitHub repository, replacing the previous reliance on the latest release.", "summary": "Fly now uses the latest tag version from the concourse/concourse GitHub repository, replacing the previous reliance on the latest release. This change affects how fly's version is determined and may require manual intervention if the version is pinned. The get_github_latest_tag function has been updated to fetch the latest tag version, and the main function now uses this function to determine the version.", "version": 1}
