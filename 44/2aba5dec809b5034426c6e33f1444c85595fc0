{"category": "Changed", "changelog_entry": "Modernized the deployment setup to use Docker and Docker Compose, enabling flexible and scalable deployment configurations with support for both SQLite and MySQL/MariaDB databases.", "summary": "The application now uses Docker and Docker Compose for deployment, replacing the previous Makefile-based setup. The Docker Compose configuration includes a MariaDB database and a pkg service that uses the database. The application's configuration has been refactored to use environment variables, and the database connection logic has been updated to support both SQLite and MySQL/MariaDB engines. The primary user outcome is improved deployment flexibility and scalability.", "version": 1}
