{"category": "Changed", "changelog_entry": "Updated qemu dependencies to ensure correct installation and usage on certain Linux distributions by adding a check for the qemu-img command and updating package dependencies.", "summary": "Qemu dependencies were updated to ensure correct installation and usage. The change involves adding a check for the qemu-img command and updating package dependencies for certain Linux distributions. This update does not introduce any breaking behavior or API changes, and it does not affect the config schema or security. The primary outcome is a more reliable and consistent installation process.", "version": 1}
