{"category": "Changed", "changelog_entry": "The server application has been modernized to initialize the database separately from handling API requests.", "summary": "The server application has been split into different phases, with the `init` phase responsible for initializing the database and the `serve` phase handling API requests. The `init` phase now runs as a separate Docker container, and the `serve` phase is run in a separate container. This change does not affect the API or CLI contract, and there are no security implications. The primary user outcome is that the database is now initialized before the API is served.", "version": 1}
