{"category": "Changed", "changelog_entry": "Pinned versions of dependent packages are now managed through a separate file, allowing for easier version control and updates in CI environments.", "summary": "Pinned versions of dependent packages are now managed through a separate file, allowing for easier version control and updates in CI environments. This change does not introduce any breaking behavior or API changes, but it does require updating the pinned versions file when dependencies are upgraded. The pinned_versions.env file contains the current pinned versions of various packages.", "version": 1}
