{"category": "Removed", "changelog_entry": "The version comparison in the kind install script now accurately matches versions with and without the 'v' prefix, preventing unnecessary reinstalls.", "summary": "The version comparison in the kind install script was modified to remove a redundant 'v' prefix from the output of kind --version. This change prevents unnecessary reinstalls by ensuring the installed version is accurately compared to the expected format. The comparison now correctly matches versions with and without the 'v' prefix.", "version": 1}
