{"category": "Changed", "changelog_entry": "Tests are now filtered out for specific operating system versions to prevent known issues and improve overall test reliability.", "summary": "CI now skips tests on certain operating systems due to an OS blacklist. The blacklist is defined in a new os-blacklist.conf file and is used to filter out tests for specific OS versions. This change affects users who run tests on affected OS versions, requiring no migration steps. The blacklist is used to prevent tests from running on known problematic OS versions, improving overall test reliability.", "version": 1}
