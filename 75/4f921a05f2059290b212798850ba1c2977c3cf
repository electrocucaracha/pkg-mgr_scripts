{"category": "Changed", "changelog_entry": "Updated Travis CI versions to ensure compatibility with the latest dependencies, now exported as environment variables in the script.", "summary": "Travis CI versions were updated in the build script to ensure compatibility with the latest dependencies. Specifically, Vagrant was updated to version 2.2.14 and GoLang to version 1.15.4. This change does not introduce any breaking behavior or API changes, but it may require updating the build environment to use the new versions. The updated versions are now exported as environment variables in the script.", "version": 1}
