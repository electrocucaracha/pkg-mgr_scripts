{"category": "Changed", "changelog_entry": "Renamed the install_pmdk function to _install_pmdk, a minor change that does not affect the installation process or user workflows.", "summary": "The install_pmdk function was renamed to _install_pmdk, which is a minor change that does not affect the installation process. The function's purpose remains the same, installing the Persistent Memory Development Kit, and its behavior is unchanged. This change is likely a refactoring step to follow a naming convention, but it does not impact any user or maintainer workflows.", "version": 1}
