{"category": "Changed", "changelog_entry": "Enabled kind autocomplete function allowing users to access kind commands through their shell.", "summary": "The kind autocomplete function has been enabled, allowing users to access kind commands through their shell. This change involves updating the main.sh script to install kind completion and the validate.sh script to test for kind autocomplete installation. The Vagrantfile has also been modified to use a provisioned shell script for kind installation and validation.", "version": 1}
