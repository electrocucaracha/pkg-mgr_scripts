{"category": "Fixed", "changelog_entry": "Improved installation robustness and developer experience by enabling adaptive hardware detection, streamlining package management, and simplifying privilege requirements for tools and QAT driver installation.", "summary": "Hardware detection and symlink handling for tools have been improved. The QAT driver installation now checks for Intel QAT hardware and skips installation if none is detected, reducing false negatives in CI. Go and Rust installers now symlink binaries into /usr/local/bin, and Skopeo and Podman installers use system packages for Ubuntu 20.04+, avoiding external repos. The Makefile no longer requires sudo for linting, and QEMU installer uses the package manager by default.", "version": 1}
