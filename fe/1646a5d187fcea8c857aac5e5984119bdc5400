{"category": "Added", "changelog_entry": "Simplified the installation process by enabling one-command installation of the package and its dependencies.", "summary": "A script for one-command installation was added, allowing users to install the package and its dependencies with a single command. The script, bindep_install.sh, checks for the bindep package and installs it if necessary. It then uses bindep to determine the required dependencies and installs them. This change simplifies the installation process for users.", "version": 1}
