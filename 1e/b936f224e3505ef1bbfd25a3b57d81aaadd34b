{"category": "Changed", "changelog_entry": "The `install_pmdk` function was renamed to `_install_pmdk` in the QEMU installation script, with no impact on the API contract or security, and requiring only a script update to use the new function name.", "summary": "The `install_pmdk` function was renamed to `_install_pmdk` in the QEMU installation script. This change affects Ubuntu 20.04 and later versions, where the function is no longer called. The API contract for installing PMDK remains unchanged, and no security impact or config schema changes are introduced. The migration requirement is to update the installation script to use the new function name.", "version": 1}
