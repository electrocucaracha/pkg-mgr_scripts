{"category": "Changed", "changelog_entry": "Updated the pinned versions in the CI environment file to use Gomplate version 3.10.0, which may require migration steps if previously pinned versions were being manually managed.", "summary": "Pinned versions in the CI environment file were updated. The version of Gomplate was bumped from 3.9.0 to 3.10.0, which may require migration steps if previously pinned versions were being manually managed.", "version": 1}
