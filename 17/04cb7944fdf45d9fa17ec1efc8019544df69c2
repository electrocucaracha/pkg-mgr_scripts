{"category": "Changed", "changelog_entry": "CI workflows now determine whether to run long tests based on the presence of the `RUN_LONG_TESTS` variable, replacing the previously used `RUN_KVM_TESTS` variable.", "summary": "CI workflows were updated to run on fewer operating systems and with a new environment variable. The `RUN_KVM_TESTS` variable is no longer used, and the `RUN_LONG_TESTS` variable is now used to determine whether to run long tests. This change may require migration steps to update existing workflows.", "version": 1}
