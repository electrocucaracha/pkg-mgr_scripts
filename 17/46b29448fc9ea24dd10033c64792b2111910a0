{"category": "Added", "changelog_entry": "Enabled support for libguestfs, allowing users to install and use it with the tool without any breaking changes or migration requirements.", "summary": "libguestfs was added to the list of supported packages, which means users can now install and use it with the tool. This change does not affect breaking behavior or migration requirements. The supported list for various package managers has been updated to include libguestfs, specifying its package names for Suse, Debian, and RedHat.", "version": 1}
