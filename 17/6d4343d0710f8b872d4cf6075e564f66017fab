{"category": "Changed", "changelog_entry": "TravisCI job configurations now explicitly exclude CentOS 7 due to unsupported podman service, resulting in a warning and error for users running podman on this OS.", "summary": "TravisCI job configuration was updated to exclude CentOS 7 due to unsupported podman service. This change affects users running podman on CentOS 7, who will encounter a warning and error when trying to start the podman service. The os-blacklist.conf file was also updated to include CentOS 7.", "version": 1}
