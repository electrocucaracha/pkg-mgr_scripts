{"category": "Changed", "changelog_entry": "The GITHUB_STEP_SUMMARY variable is now optional and defaults to a fallback path if not set.", "summary": "The GITHUB_STEP_SUMMARY variable is now optional and defaults to a fallback path if not set. This change allows the script to run without the variable being bound, but may impact users relying on the variable's original behavior.", "version": 1}
