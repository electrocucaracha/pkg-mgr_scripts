{"category": "Added", "changelog_entry": "Enabled the vagrant-reload plugin in the CI process, introducing a new dependency for users who rely on vagrant plugins and requiring them to install vagrant-reload when running CI checks.", "summary": "The CI process now includes the vagrant-reload plugin, which is installed alongside vagrant-libvirt. This change affects users who rely on vagrant plugins, as it introduces a new dependency. The migration path is to ensure vagrant-reload is installed when running CI checks.", "version": 1}
