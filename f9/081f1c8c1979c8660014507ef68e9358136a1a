{"category": "Removed", "changelog_entry": "Simplified the Vagrantfile setup process by eliminating rsync-specific configuration and defaulting to the synced folder type.", "summary": "Rsync instructions were removed from the kustomize Vagrantfile, simplifying the setup process. This change removes a dependency on rsync, allowing users to use the default synced folder type. The Vagrantfile now uses the default settings for synced folders, eliminating the need for rsync-specific configuration.", "version": 1}
