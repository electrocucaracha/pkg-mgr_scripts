{"category": "Removed", "changelog_entry": "Simplified the configuration schema by eliminating the PKG_CPU_ARCH environment variable, which previously determined the CPU architecture for binary installation, resulting in reduced complexity and potential for errors.", "summary": "The PKG_CPU_ARCH environment variable has been removed, which previously determined the CPU architecture for binary installation. This change does not introduce breaking behavior or migration requirements, nor does it affect the API or CLI contract. The config schema has been simplified by removing this variable, and the primary user outcome is a reduction in complexity and potential for errors.", "version": 1}
