{"category": "Changed", "changelog_entry": "Enabled the creation of a containerized version of the package manager through the addition of a Dockerfile, allowing users to build and run the application using Docker commands without affecting existing Makefile commands.", "summary": "A Dockerfile was added to the project, allowing for the creation of a containerized version of the package manager. This change introduces a new way to build and run the application, but it does not affect the existing Makefile commands. The Dockerfile uses a multi-stage build to create a lightweight image and exposes port 3000. Users can now build and run the package manager using Docker commands.", "version": 1}
