{"category": "Changed", "changelog_entry": "Enabled devcontainer development for kind script, allowing users to run the script within a development container environment.", "summary": "kind script now supports devcontainer development through a feature configuration, enabling users to run the script within a development container environment. This change introduces a new devcontainer-feature.json file and updates the install.sh script to accommodate devcontainer support. The kind script's functionality remains unaffected, but users can now choose to run it within a devcontainer for development purposes.", "version": 1}
