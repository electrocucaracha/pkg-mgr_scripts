{"category": "Changed", "changelog_entry": "Simplified the Docker script to support rootless mode, allowing developers to execute containers without root privileges.", "summary": "The Docker script has been improved with significant changes to support rootless mode, including the creation of a new `rootless` Docker context, installation of Docker in rootless mode, and validation of non-root container execution. The `install.sh` script now checks for Debian Jessie and Ubuntu 16.04, and the `validate.sh` script has been modified to test rootless mode. Additionally, a new `os-blacklist.conf` file has been added to blacklist Debian Jessie.", "version": 1}
