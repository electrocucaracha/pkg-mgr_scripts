{"category": "Changed", "changelog_entry": "The `GetScript` method now returns a more compact representation of the script, while the `CreateScript` method returns the created script object, enabling easier handling of script data.", "summary": "Extractor method implemented to reduce space for scripts, allowing for more efficient storage and retrieval of bash instructions. This change impacts the API by modifying the behavior of the `GetScript` method, which now returns a more compact representation of the script. The `CreateScript` method also changes to return the created script object, enabling easier handling of script data.", "version": 1}
