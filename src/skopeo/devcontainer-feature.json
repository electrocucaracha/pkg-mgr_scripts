{
  "name": "skopeo",
  "id": "skopeo",
  "version": "0.0.1",
  "description": "skopeo is a command-line utility for operations on container images and image repositories.",
  "options": {
    "pkg_debug": {
      "type": "boolean",
      "default": true,
      "description": "Enables verbosity level"
    }
  }
}
