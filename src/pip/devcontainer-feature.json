{
  "name": "pip",
  "id": "pip",
  "version": "0.0.1",
  "description": "pip is the standard package-management system used to install and manage Python packages from PyPI.",
  "options": {
    "pkg_debug": {
      "type": "boolean",
      "default": true,
      "description": "Enables verbosity level"
    }
  }
}
