{
    "name": "Kubernetes in Docker",
    "id": "kind",
    "version": "0.0.1",
    "description": "Kubernetes in Docker Command Line",
    "options": {
        "pkg_kind_version": {
            "type": "string",
            "description": "Specifies the KinD version to be installed"
        },
        "pkg_debug": {
            "type": "boolean",
            "default": "true",
            "description": "Enables verbosity level"
        }
    }
}
