{
  "name": "Docker",
  "id": "docker",
  "version": "0.0.1",
  "description": "Docker is a platform designed to help developers build, share, and run modern applications.",
  "options": {
    "pkg_docker_version": {
      "type": "string",
      "description": "Specifies the Docker version to be installed",
      "default": "latest" // PKG_DOCKER_VERSION
    },
    "pkg_debug": {
      "type": "boolean",
      "default": true,
      "description": "Enables verbosity level"
    }
  }
}
