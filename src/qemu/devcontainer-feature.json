{
  "name": "QEMU",
  "id": "qemu",
  "version": "0.0.1",
  "description": "QEMU is a hosted virtual machine monitor and machine emulator.",
  "options": {
    "pkg_debug": {
      "type": "boolean",
      "default": true,
      "description": "Enables verbosity level"
    }
  }
}
