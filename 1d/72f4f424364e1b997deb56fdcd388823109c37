{"category": "Changed", "changelog_entry": "Hardcoded versions of Go, Kind, and Terraform are now used in the CI environment, eliminating the dynamic versioning mechanism and resulting in a more consistent and predictable build process.", "summary": "Dynamic versioning for TravisCI has been disabled. This change affects the versioning of Go, Kind, and Terraform in the CI environment. The versions of these tools are now hardcoded, removing the dynamic versioning mechanism. This change does not introduce any breaking behavior or migration requirements. The primary user outcome is a more consistent and predictable build environment.", "version": 1}
