{"category": "Changed", "changelog_entry": "Enabled support for devcontainers in the CI workflow by introducing separate validation jobs for devcontainer and vagrant scripts.", "summary": "CI workflow updated to support devcontainers and vagrant scripts. The `on-demand.yml` workflow now includes separate jobs for devcontainer and vagrant validation, with a new `generate-matrix` job to create test matrices for both. The `check` job has been replaced with `check-vagrant`, and a new `check-devcontainer` job has been added.", "version": 1}
