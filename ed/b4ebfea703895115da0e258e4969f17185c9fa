{"category": "Changed", "changelog_entry": "Modernized Docker setup in CentOS 7 to support rootless mode, allowing the Docker daemon to run without root privileges and requiring users and maintainers to migrate to the new setup by creating a rootless Docker context.", "summary": "Docker setup in CentOS 7 was updated to accommodate rootless mode, which allows the Docker daemon to run without root privileges. This change affects users and maintainers by requiring a migration to the new setup, which involves creating a rootless Docker context. The update also modifies the validation script to support rootless mode, ensuring that the Docker daemon is properly configured and running.", "version": 1}
