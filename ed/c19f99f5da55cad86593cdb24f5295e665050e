{"category": "Changed", "changelog_entry": "The Go version retrieval process has been optimized to handle temporary server unavailability by introducing a retry mechanism that fetches the version up to 5 times with a 2-second delay between attempts, ensuring the latest version is obtained and allowing scripts that rely on the `go-lang` package to recover from server issues.", "summary": "The Go version retrieval process was modified to handle temporary server unavailability and ensure the latest version is obtained. A loop was added to retry fetching the version up to 5 times with a 2-second delay between attempts. This change affects scripts that rely on the `go-lang` package, particularly those that validate the installed Go version. The update ensures the package can recover from temporary server issues.", "version": 1}
