{"category": "Changed", "changelog_entry": "Enabled the installation of multiple Krew plugins at once through the kubectl script, allowing users to specify a comma-separated list of plugins to install.", "summary": "The ability to install Krew plugins was added to the kubectl script, allowing users to install multiple plugins at once. This change introduces a new environment variable PKG_KREW_PLUGINS_LIST, which specifies a comma-separated list of plugins to install. The script now iterates over this list and installs each plugin individually, replacing the previous manual installation of the tree plugin.", "version": 1}
