{"category": "Changed", "changelog_entry": "The terraform script now automatically detects and uses the correct CPU architecture to download the Terraform package.", "summary": "The terraform script now determines the CPU architecture and uses it to download the correct Terraform package. This change affects users who may need to run the script on different architectures, such as ARM, and requires no migration steps. The script's behavior is not breaking, and the API or CLI contract remains unchanged. The CPU architecture is now automatically detected and used to download the Terraform package.", "version": 1}
