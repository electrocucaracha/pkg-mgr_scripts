{"category": "Fixed", "changelog_entry": "Improved pip command availability and validation by enabling the use of python -m pip, which ensures the correct interpreter is used and works even if only python or python3 is available.", "summary": "The pip command is now available as a symlink to pip3, preserving compatibility for legacy scripts, and validation uses python -m pip instead of invoking pip directly, ensuring the correct interpreter is used and improving reliability of pip installation and validation steps. This change addresses issues where only pip3 is installed, and tools expect a plain pip command. The validation approach is now more robust, working even if only python or python3 is available.", "version": 1}
