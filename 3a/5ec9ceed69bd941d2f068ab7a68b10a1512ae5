{"category": "Changed", "changelog_entry": "Updated kubectl to enable autocomplete functionality, providing users with command suggestions while typing.", "summary": "kubectl autocomplete functionality has been enabled, allowing users to access command suggestions while typing. This change involves updating the main.sh script to install and configure kubectl completion, as well as modifying the validate.sh script to test for the presence of the autocomplete functions. The kubectl version is now checked against the stable version from the Kubernetes release, and the installation of kubectl and krew plugins is also updated.", "version": 1}
