{"category": "Changed", "changelog_entry": "Updated the pinned versions of various packages and distributions used in the build and test environments, reflecting the changes in the pinned_versions.env file and the distros_supported.yml file.", "summary": "The pinned versions of various packages and distributions were updated in the environment and distribution configuration files. This change affects the build and test environments, as it updates the versions of dependencies and distributions used in the CI pipeline. The updated versions are reflected in the pinned_versions.env file and the distros_supported.yml file.", "version": 1}
