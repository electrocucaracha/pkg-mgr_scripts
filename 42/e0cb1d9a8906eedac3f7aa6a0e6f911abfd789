{"category": "Changed", "changelog_entry": "Updated the default QEMU version to 5.2.0, requiring a corresponding update in the environment variable PKG_QEMU_VERSION.", "summary": "QEMU version was bumped to 5.2.0, requiring a corresponding update in the default version specified in the environment variable PKG_QEMU_VERSION. The main.sh script was updated to fetch the latest Ninja version from GitHub and install it if necessary. The changes also include minor adjustments to package installation commands and paths.", "version": 1}
