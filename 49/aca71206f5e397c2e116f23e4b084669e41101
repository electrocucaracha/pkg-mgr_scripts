{"category": "Changed", "changelog_entry": "pip is now the recommended package manager on Debian and RedHat distributions, and the installation script will automatically install it if necessary for certain packages.", "summary": "The installation script now supports pip as a package manager, which is required for certain packages. This change affects Debian and RedHat distributions, where pip is now the recommended package manager. Users on these platforms must have pip installed to install certain packages, and the script will automatically install it if necessary.", "version": 1}
