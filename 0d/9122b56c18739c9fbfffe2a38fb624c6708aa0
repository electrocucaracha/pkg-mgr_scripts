{"category": "Changed", "changelog_entry": "Tests are now grouped by profile, with the selected profile determining which tests to execute based on a configurable list of profiles defined by the `PROFILE` environment variable.", "summary": "Tests are now grouped by profile, with profiles defined in a configurable list. This change affects how tests are run, with the `PROFILE` environment variable determining which tests to execute. The `ci_tests` variable is now populated dynamically based on the selected profile. This change may require adjustments to test configurations and workflows.", "version": 1}
