{"category": "Changed", "changelog_entry": "Modernized the kubectl script to dynamically generate download URLs for kubectl and krew binaries based on the system's CPU architecture.", "summary": "The kubectl script now determines the CPU architecture automatically using the `get_cpu_arch` function, replacing hardcoded values. This change affects the download URLs for kubectl and krew binaries, which are now dynamically generated based on the system's architecture. The script's behavior remains unchanged for most users, but this update ensures compatibility with different CPU architectures.", "version": 1}
