{"category": "Added", "changelog_entry": "Enabled local sudo fallback in all install and validate scripts, allowing installers to work seamlessly whether run as root or a non-root user and ensuring correct permissions in multi-user environments.", "summary": "Installers for various tools now provide a local sudo fallback to work seamlessly whether run as root or a non-root user. This change addresses failures in devcontainer test images that execute feature installers as root without sudo. The fallback is implemented in all install and validate scripts, ensuring correct permissions in multi-user environments and preventing group assignment errors.", "version": 1}
