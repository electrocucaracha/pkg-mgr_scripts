{"category": "Changed", "changelog_entry": "The go-lang script now retrieves the latest Go version from the official Go website, ensuring it always uses the latest available version.", "summary": "The go-lang script has been refactored to retrieve the latest Go version from the official Go website. This change introduces a new function, `get_go_latest_version`, which replaces the previous version retrieval logic in the `main` function. The script now uses this function to set the `version` variable, ensuring it always uses the latest available version.", "version": 1}
