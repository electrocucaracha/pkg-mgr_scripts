{"category": "Changed", "changelog_entry": "The default permissions for bash executables are now set correctly by the spec installation process, enabling it to function as intended.", "summary": "The default permissions for bash executables have been changed to executable mode, affecting the spec installation process. This change requires no migration steps, as it is a one-time adjustment. The spec installation process now correctly sets executable permissions for bash scripts.", "version": 1}
