{"category": "Changed", "changelog_entry": "Enabled the gVisor sandbox as a runtime option for Docker installations, allowing containers to be run in the sandbox via the `--runtime=runsc` option.", "summary": "Docker installation now includes gVisor sandbox installation, adding a new runtime option. The `runsc` command is installed and made available as a Docker runtime, and the `docker run` command can now use the `--runtime=runsc` option to run containers in the gVisor sandbox. This change does not affect existing Docker installations or configurations.", "version": 1}
