{"category": "Changed", "changelog_entry": "Updated the CI version for PKG_KN_VERSION to 0.23.0 in the pinned versions configuration, with no breaking behavior, API or CLI changes, or security impact, and no migration steps required.", "summary": "The CI version for PKG_KN_VERSION has been updated from 0.22.0 to 0.23.0 in the pinned versions configuration. This change does not introduce any breaking behavior, API or CLI changes, or security impact. The config schema remains unchanged, and no migration steps are required. The primary outcome is an updated version of PKG_KN_VERSION in the CI environment.", "version": 1}
