{"category": "Changed", "changelog_entry": "Optimized integration test execution by allowing multiple test scenarios to run simultaneously.", "summary": "Integration tests now run in parallel, enabling faster execution of multiple test scenarios simultaneously. This change involves updating scripts to utilize the `nohup` command and introducing a new `run_long_tests.sh` script to manage parallel test execution. The `parallel-tests.txt` file has been added to specify test scenarios to run in parallel. This update does not introduce breaking behavior or API changes, but it does require migration to the new parallel test execution mechanism.", "version": 1}
