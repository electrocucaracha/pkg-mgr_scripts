{"category": "Changed", "changelog_entry": "Enabled support for running the kubectl script within a development container environment.", "summary": "kubectl script now supports development container features, allowing users to run the script within a containerized environment. This change introduces a new devcontainer-feature.json file and updates the README.md to include dev container configuration. The install.sh script has also been modified to accommodate the new dev container feature, but no breaking changes or migration requirements are introduced.", "version": 1}
