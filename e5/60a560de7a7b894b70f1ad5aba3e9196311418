{"category": "Changed", "changelog_entry": "The CPU architecture for installing kind and kubectl binaries is now determined by the PKG_CPU_ARCH environment variable or the system architecture.", "summary": "The `PKG_CPU_ARCH` environment variable has been added to allow users to override the CPU architecture used for installing some binaries. This change affects the installation of kind and kubectl, where the architecture is now determined by the `PKG_CPU_ARCH` variable or the system architecture. The `apt-get update` command has also been modified to run silently.", "version": 1}
