{"category": "Added", "changelog_entry": "Enabled the NFS installation script for Ubuntu Bionic to correctly install the nfs-kernel-server package by resolving a dependency issue with apt update.", "summary": "The NFS installation script for Ubuntu Bionic was updated to correctly install the nfs-kernel-server package. This change involves running apt update before installing the package, which is necessary due to a change in how package dependencies are handled on Ubuntu Bionic. The script's behavior remains unchanged for other supported distributions.", "version": 1}
