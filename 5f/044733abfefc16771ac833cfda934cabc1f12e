{"category": "Fixed", "changelog_entry": "Resolved unnecessary Go version validation failures by allowing prefix matching for version checks.", "summary": "The Go version validation script now allows for prefix matching, enabling minor version pinning. This change addresses a limitation where exact version matches were required, causing unnecessary validation failures when patch versions were updated. The script now correctly handles cases where the installed version matches the expected version as a prefix, improving validation accuracy.", "version": 1}
