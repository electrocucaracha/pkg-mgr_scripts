{"category": "Changed", "changelog_entry": "Updated the dependency on actions/checkout to version 4.2.1, which does not introduce any breaking behavior or migration requirements.", "summary": "The dependency on actions/checkout was updated from version 4.2.0 to 4.2.1. This change affects multiple workflows that use actions/checkout, but it does not appear to introduce any breaking behavior or migration requirements. The API or CLI contract has not changed, and there is no mention of security impact or config schema changes. The primary user outcome is that the workflows will now use the updated version of actions/checkout.", "version": 1}
