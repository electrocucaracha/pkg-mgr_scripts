{"category": "Added", "changelog_entry": "pip installations can now be made without modifying the system PATH environment variable.", "summary": "pip installation now supports non-PATH installations, allowing users to install the package without modifying their system PATH environment variable. This change affects users who install the package via pip, as they no longer need to ensure the package is installed in a directory included in their system's PATH. The installation script has been updated to handle this scenario, and the validate script now checks for the presence of the tox package.", "version": 1}
