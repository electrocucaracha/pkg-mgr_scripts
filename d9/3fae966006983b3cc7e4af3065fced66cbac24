{"category": "Added", "changelog_entry": "The docker chameleonsocks script now depends on the wget package, which is installed if it is not already present, and users who run the script without wget installed must install it before proceeding.", "summary": "The docker chameleonsocks script now depends on the wget package, which is installed if it is not already present. This change affects users who run the script without wget installed, requiring them to install it before proceeding. The script installs wget using a curl download if it is not available.", "version": 1}
