{"category": "Changed", "changelog_entry": "The root Docker configuration file is now properly updated in the root directory when the script is run with elevated privileges.", "summary": "The root Docker configuration file is now copied from the user's home directory to the root directory only when the script is run with elevated privileges. This change ensures that the configuration file is properly updated in the root directory, but it may require additional steps to migrate existing configurations. The Docker configuration file's location remains unchanged, and no security implications are introduced by this change.", "version": 1}
