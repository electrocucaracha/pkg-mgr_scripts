{"category": "Changed", "changelog_entry": "Enabled the yamllint tool for YAML syntax checks in project configuration files, with no impact on the project's API or CLI contract.", "summary": "The yamllint tool has been enabled for code validation. This change affects the tox.ini file, where yamllint has been added to the test environment dependencies. The tool will be used to check for YAML syntax errors in the project's configuration files. Migration steps are not required, and the change does not impact the project's API or CLI contract.", "version": 1}
