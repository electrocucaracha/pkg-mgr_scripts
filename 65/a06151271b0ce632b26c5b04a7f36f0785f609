{"category": "Changed", "changelog_entry": "Optimized on-demand CI task workflows to only trigger on specific paths being pushed or pulled, reducing unnecessary runs and improving overall performance.", "summary": "CI workflows for on-demand tasks were improved. The main change is that the workflows now only trigger on specific paths being pushed or pulled, reducing unnecessary runs. The workflows also now use a more consistent naming convention and have been split into separate files for each profile. This change should improve performance and reduce noise in the CI pipeline.", "version": 1}
