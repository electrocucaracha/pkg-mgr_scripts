{"category": "Removed", "changelog_entry": "Eliminated the self-referential invocation of the check.sh script, which no longer needs to be run as part of the build process.", "summary": "The check.sh script has been modified to install additional dependencies and configure the system for integration testing. This change breaks the previous behavior of installing only a subset of dependencies, and users must now manually update their configuration to install the required packages. The script now installs vagrant, libvirt, and other dependencies, and configures the system for integration testing.", "version": 1}
