{"category": "Changed", "changelog_entry": "Upgraded actions/checkout to version 3.5.0, ensuring workflows that use this dependency continue to function without breaking behavior or requiring migration.", "summary": "The dependency on actions/checkout was updated from version 3.4.0 to 3.5.0. This change affects various workflows that use actions/checkout, including compress-images.yml, distros.yml, lint.yml, on-demand.yml, rebase.yml, release.yml, and spell.yml. The update does not introduce breaking behavior or migration requirements, nor does it change the API or CLI contract, config schema, or security impact. The primary outcome is an updated dependency.", "version": 1}
