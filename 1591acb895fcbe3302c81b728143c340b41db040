{"category": "Changed", "changelog_entry": "Updated dictionary definitions and incremented pinned package versions in CI and devcontainer configurations to reflect the changes.", "summary": "Dictionary definitions were updated and pinned versions of various packages were incremented in CI and devcontainer configurations. The update excludes spec/ files from shfmt formatting and reverts install_spec.sh. The changes affect CI workflows, devcontainer configurations, and package versions.", "version": 1}
