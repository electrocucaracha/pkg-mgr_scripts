{"category": "Changed", "changelog_entry": "Enabled podman to run in rootless mode on supported Linux distributions, allowing users to run containers without root privileges.", "summary": "Podman is now enabled to run in rootless mode, allowing users to run containers without root privileges. This change affects Ubuntu 18.04 and CentOS 7 and 8, which are no longer supported for rootless mode. The podman service is now managed using systemd socket activation, and the loginctl command is used to enable linger for the user.", "version": 1}
