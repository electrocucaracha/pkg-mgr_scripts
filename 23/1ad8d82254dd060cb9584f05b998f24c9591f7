{"category": "Changed", "changelog_entry": "Modernized the default Docker context to support non-root container execution.", "summary": "Docker rootless experimental feature is enabled, allowing for non-root container execution. This change requires users to switch to the rootless context and validate its functionality. The validation process includes testing root and non-root container execution with both rootless and default contexts. No breaking behavior or API changes are introduced, but users must adapt to the new rootless context.", "version": 1}
