{"category": "Changed", "changelog_entry": "Optimized the installation script for libvirt to correctly handle Rocky Linux, resulting in improved socket access permissions and package installation.", "summary": "The installation script for libvirt was modified to accommodate different Linux distributions, specifically to handle Rocky Linux correctly. This change affects the permissions for socket access and the installation of required packages. The script now sets the permissions for socket access to 0770 and installs libvirt-devel on non-Rocky Linux systems. The installation process remains largely unchanged.", "version": 1}
