{"category": "Fixed", "changelog_entry": "Stabilized the installation process for Rocky Linux by ensuring the Docker CE repo can be added on fresh systems and verifying the docker CLI version after installation.", "summary": "The script now explicitly installs dnf-plugins-core for Rocky Linux compatibility, ensuring the Docker CE repo can be added on fresh systems. This change also updates the test to verify the docker CLI version after installation, providing a more reliable indicator of success. The docker CLI version is now checked directly, replacing environment variable checks.", "version": 1}
