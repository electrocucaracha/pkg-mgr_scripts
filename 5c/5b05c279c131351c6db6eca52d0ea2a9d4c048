{"category": "Changed", "changelog_entry": "Improved Docker CI validation to provide more informative output and warn instead of erroring if Docker autocomplete functions are not installed.", "summary": "Docker CI validation has been improved by adding a new validation step and modifying the existing one to provide more informative output. The new step runs the `validate.sh` script and logs its output, while the existing step now warns instead of errors if Docker autocomplete functions are not installed. This change does not introduce any breaking behavior or migration requirements.", "version": 1}
