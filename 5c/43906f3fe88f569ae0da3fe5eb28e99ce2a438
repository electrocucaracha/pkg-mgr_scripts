{"category": "Changed", "changelog_entry": "The virtualbox script was modernized to support CentOS 8 Stream, requiring users to update their virtualbox setup to use the new script.", "summary": "The virtualbox script was updated to support CentOS 8 Stream by adding a new provision to reload the environment and modifying the package manager to use `dnf` instead of `yum`. The script now also checks for the existence of the `_requirements.sh` file and sources it if present. This change affects users of CentOS 8 Stream, who will need to update their virtualbox setup to use the new script.", "version": 1}
