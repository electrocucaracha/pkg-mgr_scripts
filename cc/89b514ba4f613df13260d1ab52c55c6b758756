{"category": "Changed", "changelog_entry": "crun version validation is now accurately checked by properly comparing the installed version with the expected one.", "summary": "Crun version validation was fixed to correctly compare the installed version with the expected one. The change affects how the script checks for a compatible crun version, ensuring that the validation is accurate. The fix modifies the version comparison logic to properly trim the version string.", "version": 1}
