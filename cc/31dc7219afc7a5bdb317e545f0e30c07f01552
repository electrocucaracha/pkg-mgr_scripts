{"category": "Changed", "changelog_entry": "Enabled custom actions for CI checks by introducing a new check action and modifying existing workflows to use it, reducing repetitive setup and test steps.", "summary": "Vagrant setup and check custom actions were created. The actions, `check` and `vagrant-setup`, are used in various workflows to set up and configure Vagrant environments for CI checks. The `check` action runs integration tests, checks hardware resources, and logs, while the `vagrant-setup` action installs and configures tools required for the CI check composite task. The changes introduce a new custom action, `check`, and modify existing workflows to use it, replacing repetitive setup and test steps.", "version": 1}
