{"category": "Added", "changelog_entry": "Enabled users to customize the version of the regclient package by externalizing the PKG_REGCLIENT_VERSION variable, which can now be set as an environment variable.", "summary": "PKG_REGCLIENT_VERSION is now an environment variable that can be set externally, allowing users to customize the version of the regclient package. This change affects the Docker environment, where the variable is used to set the version in the Docker image. The change does not introduce any breaking behavior or security risks.", "version": 1}
