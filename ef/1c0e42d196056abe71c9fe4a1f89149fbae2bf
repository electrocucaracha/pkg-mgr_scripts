{"category": "Changed", "changelog_entry": "pip now uses the --no-cache-dir option by default when installing packages, which can improve performance by reducing the amount of data pip needs to store.", "summary": "The installation script now uses the `--no-cache-dir` option when installing packages with pip, which can improve performance by reducing the amount of data pip needs to store. This change affects users who rely on pip for package management, and it does not require any migration steps. The API or CLI contract remains unchanged, and there is no security impact or config schema change.", "version": 1}
