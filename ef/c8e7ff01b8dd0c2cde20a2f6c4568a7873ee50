{"category": "Removed", "changelog_entry": "The scripts now use the `uname` command to determine the OS and architecture, and then use sed to map the architecture to a specific string, replacing the previously used get_cpu_arch function.", "summary": "The `get_cpu_arch` function was removed from various scripts. This change affects how CPU architectures are detected and used in the installation process for several tools, including CNI plugins, Docker, Fly, Go, Gomplate, Kind, Knative client, kubectl, kustomize, Podman, QEMU, Terraform, and Vagrant. The scripts now use the `uname` command to determine the OS and architecture, and then use sed to map the architecture to a specific string. This change is likely to break installations on systems that rely on the `get_cpu_arch` function.", "version": 1}
