{"category": "Changed", "changelog_entry": "CNI plugins script now supports development containers through a new feature configuration, enabling users to easily test and develop CNI plugins within a containerized environment.", "summary": "CNI plugins script now supports development containers through a new feature configuration, enabling users to easily test and develop CNI plugins within a containerized environment. This change introduces a new devcontainer-feature.json file and updates the install.sh script to accommodate development container setup. The validate.sh script also receives modifications to support the new development container feature.", "version": 1}
