{"category": "Changed", "changelog_entry": "Workflows are now triggered consistently on push and pull requests, including all image files and branches, regardless of type.", "summary": "GitHub Actions workflows were refactored to improve triggering behavior. The change affects how workflows are triggered on push and pull requests, now including all image files and all branches. The update ensures consistent triggering across all workflows, including compress-images, lint, reviewdog, and triage. This change does not introduce breaking behavior or security concerns.", "version": 1}
