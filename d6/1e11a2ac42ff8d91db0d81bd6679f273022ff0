{"category": "Changed", "changelog_entry": "Optimized the default cURL configuration to allow insecure connections by default, enabling users to bypass SSL verification for packages from untrusted sources, but be aware that this may introduce security risks if not used carefully.", "summary": "Insecure cURL calls are now enabled by default, allowing users to bypass SSL verification. This change affects users who need to install packages from untrusted sources. The insecure option is added to the global cURL configuration file, and a trap is set to remove it when the script exits. This may introduce security risks if not used carefully.", "version": 1}
