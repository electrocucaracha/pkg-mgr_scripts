{"category": "Changed", "changelog_entry": "pip installations on Python 2.7 are now incompatible due to the updated pip URL scheme.", "summary": "The pip bootstrap URL was updated to use the new pip URL scheme, which will break installations on Python 2.7. The affected users will need to update their Python version or use an older pip version. This change does not impact the API or CLI contract, nor does it introduce any security vulnerabilities.", "version": 1}
