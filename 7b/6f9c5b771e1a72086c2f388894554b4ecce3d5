{"category": "Changed", "changelog_entry": "Enabled the installation of helm plugins by requiring the presence of awk, which must be installed separately if it's not already present on the system.", "summary": "Helm installation requirements now include awk, which must be installed separately if it's not already present on the system. This change affects users who need to install helm plugins, as awk is a dependency for some of these plugins. The installation process will fail if awk is not available, prompting users to install it manually.", "version": 1}
