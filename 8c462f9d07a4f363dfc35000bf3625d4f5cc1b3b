{"category": "Fixed", "changelog_entry": "Resolves SOCKS proxy setup reliability issues for users behind a proxy by ensuring the chameleonsocks installation script receives proxy configuration variables when run with sudo.", "summary": "The chameleonsocks installation script now correctly receives proxy configuration variables when run with sudo, ensuring proper SOCKS proxy setup during Docker installation. This change addresses a reliability issue for users behind a SOCKS proxy. The fix uses the env command to pass proxy variables to the script's environment.", "version": 1}
