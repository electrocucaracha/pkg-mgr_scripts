{"category": "Added", "changelog_entry": "Enabled automated release of dev container features via a new GitHub Actions workflow that publishes features from the master branch using the `devcontainers/action`.", "summary": "A GitHub Actions workflow was added to release dev container features. This change introduces a new workflow that publishes dev container features from the master branch, using the `devcontainers/action` to generate and publish features. The workflow uses a GitHub token to authenticate the publish operation.", "version": 1}
