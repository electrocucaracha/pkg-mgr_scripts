{"category": "Fixed", "changelog_entry": "Resolved compatibility issues with scripts that include sudo flags by correcting the sudo fallback to handle options like -E or -n when invoked with sudo, and improved the CNI plugins test to check both legacy and new plugin install locations.", "summary": "The `sudo` fallback in scripts now correctly handles options like `-E` or `-n` when invoked with `sudo`. This change ensures compatibility with scripts that include `sudo` flags. Additionally, the CNI plugins test now checks both legacy and new plugin install locations, improving robustness across plugin versions.", "version": 1}
