{"category": "Changed", "changelog_entry": "Optimized podman installation in CentOS to avoid dependency issues by installing libseccomp-devel and updating podman service validation scripts, and note that the podman service is not supported in CentOS 7.", "summary": "Podman installation in CentOS was fixed to avoid dependency issues. The script now installs libseccomp-devel instead of disabling container tools and enabling specific repositories. This change affects CentOS 7 and 8, and users should note that the podman service is not supported in CentOS 7. The podman installation and service validation scripts were updated to reflect these changes.", "version": 1}
