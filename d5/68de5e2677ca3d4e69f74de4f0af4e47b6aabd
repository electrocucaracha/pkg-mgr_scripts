{"category": "Changed", "changelog_entry": "Optimized memory allocation for Vagrant virtual machines by reducing the default memory allocation from 1024 MB to 512 MB, affecting all providers including virtualbox and libvirt.", "summary": "Memory usage has been reduced by decreasing the default memory allocation for Vagrant virtual machines from 1024 MB to 512 MB. This change affects all providers, including virtualbox and libvirt. The adjustment is intended to minimize resource usage, but users may need to adjust their configurations if they require more memory.", "version": 1}
