{"category": "Changed", "changelog_entry": "The default Kustomize version has been hardened to 3.8.8, removing its dependency on fetching the latest version from GitHub.", "summary": "Kustomize version has been fixed in the installation script. The default version to be installed is now 3.8.8, and the script no longer attempts to fetch the latest version from GitHub. This change ensures that the installed version is consistent and does not depend on external resources. The installation process remains unchanged for users.", "version": 1}
