{"category": "Added", "changelog_entry": "Enabled proper recognition of runtime-related terms in spelling and terminology checks during CI, reducing false positives and improving developer experience.", "summary": "The wordlist in the project's CI configuration was updated to include the term \"runtime\", which was previously missing. This change ensures that references to runtime environments or errors are properly recognized and not flagged as spelling or terminology errors. The addition of \"runtime\" to the wordlist improves the developer experience by reducing unnecessary review noise and false positives in checks.", "version": 1}
