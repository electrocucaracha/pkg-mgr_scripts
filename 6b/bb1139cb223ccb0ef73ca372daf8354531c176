{"category": "Changed", "changelog_entry": "Vagrant installations are now updated with new binaries, potentially requiring manual intervention to resolve conflicts.", "summary": "Vagrant binaries are now overridden with new versions, potentially breaking existing installations. The installation script now uses the `-o` flag with `unzip` to overwrite existing files, and `libarchive-tools` is installed as a dependency. This change may require manual intervention to resolve conflicts.", "version": 1}
