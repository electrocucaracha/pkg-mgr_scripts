{"category": "Added", "changelog_entry": "GitHub Actions workflows now continue to run and report all job failures instead of immediately stopping on the first job failure.", "summary": "GitHub Actions workflows now fail on the first job failure instead of immediately stopping the entire workflow. This change is made by setting `fail-fast` to `false` in the `.github/workflows/on-demand_ci.yml` file. The workflow will continue to run and report all job failures.", "version": 1}
