{"category": "Added", "changelog_entry": "Enabled support for running VirtualBox on CentOS images.", "summary": "VirtualBox modules are now enabled for CentOS images during the build process, allowing users to install and run VirtualBox on these systems. This change affects the build process and requires no migration steps. The build script now imports the required kernel development packages and runs the vboxconfig command to configure the VirtualBox modules. This update enables users to run VirtualBox on CentOS images.", "version": 1}
