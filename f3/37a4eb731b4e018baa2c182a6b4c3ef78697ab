{"category": "Changed", "changelog_entry": "Enabled qemu installation on openSUSE by requiring the diffutils package to be installed alongside other dependencies.", "summary": "Qemu installation on openSUSE was fixed by adding the diffutils package to the list of dependencies. This change affects the installation process on openSUSE systems, which now require diffutils to be installed alongside other packages. The installation script has been updated to include diffutils in the list of packages to be installed.", "version": 1}
