{"category": "Changed", "changelog_entry": "Scripts are now referenced from an external directory, eliminating the need for the scripts folder within the repository.", "summary": "The scripts folder has been moved out of the repository and now resides in a separate directory. This change affects the Dockerfile and Makefile, which now reference the external scripts path. The `PKG_SCRIPTS_PATH` environment variable has been added to the Config struct in `cmd/main.go`, and the Docker Compose file has been updated to mount the external scripts volume. This change requires no migration steps.", "version": 1}
