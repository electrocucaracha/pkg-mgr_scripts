{"category": "Changed", "changelog_entry": "Enabled development container support for crystal-lang scripts, making it consumable as a feature through a specific configuration.", "summary": "Crystal-lang script now supports development containers, enabling users to consume it as a feature through a specific configuration. This change affects Ubuntu 16.04, which is now unsupported, and introduces a new devcontainer-feature.json file. The install.sh script has been modified to accommodate development container setup, and the os-blacklist.conf file has been updated to reflect the change.", "version": 1}
