{"category": "Added", "changelog_entry": "Enabled users to manage Kubernetes applications with Helm Charts through the addition of a helm package manager and supporting scripts.", "summary": "Helm installation script was added, enabling users to manage Kubernetes applications with Helm Charts. The change includes a new `helm` package manager in the installation script, and a `main.sh` script that installs and configures Helm. The `validate.sh` script checks if Helm is installed correctly.", "version": 1}
