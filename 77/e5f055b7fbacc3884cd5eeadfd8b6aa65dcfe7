{"category": "Changed", "changelog_entry": "Stabilized ctr execution to correctly target the containerd socket address, ensuring the plugins ls command functions as expected with no additional steps required.", "summary": "ctr execution now correctly targets the containerd socket address, ensuring the plugins ls command works as expected. This change affects users who rely on ctr plugins ls in debug mode, requiring no additional steps. The API/CLI contract remains unchanged, and there is no security impact or config schema change.", "version": 1}
