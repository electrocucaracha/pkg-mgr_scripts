{"category": "Changed", "changelog_entry": "The script now correctly handles user membership in the docker group, ensuring proper access for users without requiring any migration steps or changes to the config schema.", "summary": "Docker user permissions were fixed to ensure proper access, addressing a potential issue where users might not be added to the docker group correctly. The change involves modifying the script to check if the current user is already in the docker group before attempting to add them. This update should not require any migration steps or changes to the config schema.", "version": 1}
