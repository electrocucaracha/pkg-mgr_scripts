{"category": "Changed", "changelog_entry": "Simplified the local deployment process by introducing a script that automates server installation and configuration, and a new `install` target was added to the Makefile to streamline the deployment workflow.", "summary": "Local deployment instructions were added to the project, allowing users to deploy the Web Server locally for development purposes. The instructions include a script to install and configure the server, and a new `install` target was added to the Makefile to simplify the deployment process. The deployment process now involves pulling the latest images and deploying the server with the `up` command.", "version": 1}
